spring:
  datasource:
    url: jdbc:mysql://localhost:3306/comsumer2db?createDatabaseIfNotExist=true
    username: root
    password: energystar10
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  rabbitmq:
    host: localhost
    password: guest
    port: 15673
    username: guest
    exchange: user.exchange
    queue: user.queue1
    routingkey: user.routingkey1

